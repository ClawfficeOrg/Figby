# E2E Tests — Figby TUI Image Editor, Layers & Banner Finale

Two suites, split on purpose:
- **Headless structural** (`figby-rs/tests/e2e_art.rs`, 4 tests): figmap
  loads, dims, layer names/counts, frame counts, gold-cell content, light
  rig shape, `into_runtime` + `composite()`. Fast, runs in CI.
- **Non-headless PTY** (`scripts/e2e-art-tmux.sh`, 15 checks, ~1 min):
  real TUI in tmux — Open-dialog typing + Enter, canvas content, tool keys
  via status bar, timeline/export dialogs, `--play --loop` progress.
  Run: `./scripts/e2e-art-tmux.sh` (exit 0 = all pass). The PTY suite is
  the real e2e; the headless tests only pin the scene contract.

Art scenes live in `assets/e2e-art/*.figmap`, generated by
`scripts/gen-e2e-art.py` (all original tribute work — 8-bit *style*,
never ROM copies). Regenerate: `python3 scripts/gen-e2e-art.py`.
`--play` checks need a PTY (plain pipes fail with `Device not configured`).

---

## 0. Automated coverage (cargo test --test e2e_art, 4 tests green)

- [x] `e2e_art_sprites_load_with_hero_layer` — all 3 sprite figmaps load,
  40×20, layers `[Background, Hero]`, active=1, hero ≥20 painted cells,
  background >100 painted cells.
- [x] `e2e_art_castle_has_three_named_layers` — 64×20, layers
  `[Sky, Castle, Hero]`, active=2, castle ≥100 / hero ≥20 painted cells.
- [x] `e2e_art_banner_is_animation_with_lights` — kind=Animation, 64×24,
  6 frames, loop, fps=8, one 64×24 `document_state` per frame, >30 gold
  `#FFD700` cells per frame, 2 lights, 2 ordered light keyframes on light 1.
- [x] `e2e_art_banner_into_runtime_composites` — `into_runtime` keeps
  layers/timeline/lights; `composite()` returns 64×24 without panic.
- [x] `figby --play banner --loop` under tmux renders the gold FIGBY banner
  + progress bar `1/6 … 1.00x`; any key exits loop. (Static sprite figmaps
  correctly refuse: `has no animation frames to play`, exit 1.)
- [ ] Headless `--play` (no PTY) still errors `Device not configured (os
  error 6)` — expected terminal requirement, not a bug. Notes:

## 1. Image editor tools (first priority) — tribute sprites

Setup per sprite: open `assets/e2e-art/sprite-<name>-tribute.figmap`
(`Ctrl+O` → Open mode lists `.figmap`; ImageEditor `Ctrl+O` opens the
*image* dialog instead — use `Alt+F` File menu or FontEditor-mode `Ctrl+O`).
Reference scenes: plumber (night), quest (day), invader (night).

Tool keys (verified live 2026-09-23, AsciiPreview/ImageEditor modes):
`b` Brush, `u` Move, `r` Rotate, `v` Select, `l` Lasso, `c` Circle,
`p` Polygon, `g` Fill, `i` Line, `e` Eraser, `d` Eyedropper, `a` Spray,
`t` Text (see §1.8), `m` Emitter (mouse-only), `n` Lighting (see §4),
`k` Braille. `Space`/`Enter` paints at cursor for
Brush/Eraser/Line/Fill/Spray; Braille uses `Space`/`Enter` to toggle the
dot under its 2×4 sub-cursor (arrows move the dot, not the cell).

### 1.1 Brush (`b`) — recreate the hero on a blank layer
- [ ] New layer (`Layers` menu → New Layer or layer-panel `n`), select Brush
- [ ] `Space` stamps the default `█`; `[` / `]` resize 1–20, `\` cycles
  shape (Square→Circle→Spray→Custom), `;` / `'` density, `M` marker sub-mode
- [ ] Recreate the tribute sprite freehand on the dim `.` playfield
- [ ] Undo (`Ctrl+Z`) / redo (`Ctrl+Y`) removes/restores the stroke
- [ ] Notes:

### 1.2 Eraser (`e`)
- [ ] `Space` stamps `erase_stamp` with current shape+size over the sprite
- [ ] Locked layer rejects the erase (no-op, no undo entry). Notes:

### 1.3 Fill (`g`)
- [ ] `Space` on a closed region runs `flood_fill` with palette-applied cell
- [ ] Fill on the open gradient bleeds to the layer edge (expected — no
  containment). Notes:

### 1.4 Line (`i`)
- [ ] Mouse drag previews from `saved_buffer`, recomputed per-event (no drift)
- [ ] Keyboard `Space` stamps a single brush cell (line needs the mouse). Notes:

### 1.5 Selection: Select/Move/Rotate/Lasso/Circle/Polygon (`v/u/r/l/c/p`)
- [ ] Marquee drag → arrows move selection, `Delete` clears, `Ctrl+C/X/V`
  copy/cut/paste at cursor
- [ ] Move tool arrows nudge the whole layer when nothing is selected
- [ ] Rotate `Left`/`Right` steps 90°; mouse drag maps distance to steps
- [ ] Polygon: click vertices, `Enter` closes (≥3 pts), `Esc` cancels
- [ ] Notes:

### 1.6 Spray (`a`) — flair behind the sprite
- [ ] Mouse/`Space` stamps `spray_stamp` with size+density from brush state
- [ ] Recreate the `+`/`.` sparkle flair around the hero. Notes:

### 1.7 Eyedropper (`d`) + palette
- [ ] Click a sprite cell → `brush.ch` + FG color picked up, pushed to Recent
- [ ] Palette keys `x`/`X` target, `f`/`F` foreground, `h`/`H` custom hex,
  `z`/`Z` extended, arrows navigate. Notes:

### 1.8 Text (`t`) — caption the sprite
- [ ] Side-panel Text tab → type caption → `Enter` commits FIGlet block
  (real font render via `fonts/` dir, default scale 1)
- [ ] Arrows move committed block, `+`/`-` scale, `r` rotates 90°,
  `Delete` removes, `Space`/`Enter` re-edits. Notes:

### 1.9 Braille (`k`)
- [ ] Arrows move the 2×4 dot cursor inside the cell; `Space`/`Enter`
  toggles the dot via shared `toggle_braille_dot` (same bits as image import)
- [ ] Mouse click selects whole cell only (documented limitation). Notes:

### 1.10 Emitter (`m`, mouse-only — no keyboard shortcut)
- [ ] Click canvas with Emitter selected → `emitter_active`, config panel
  opens at click point (`emitter_x/y` set, system rebuilt)
- [ ] `b` while emitter active bakes particles to a `bake frame N` layer
- [ ] `A` captures composite → timeline frame (opens timeline on first capture)
- [ ] Notes:

## 2. Layers — famous-8bit-inspired background (`layers-castle-tribute.figmap`)

Scene: `Sky` (day gradient + sun + clouds) / `Castle` (keep + twin towers +
brick ground) / `Hero` (plumber tribute). Layer-panel keys below only fire
when the side drawer is open on the Layers tab (`?` toggles, `Alt+←/→`
cycles tabs); the Layers *menu* works from anywhere.

- [ ] Open drawer (`?`), Layers tab: `↑/↓` select (plain), `Alt+↑/↓` select
  (documented alt path), `Alt+Shift+↑/↓` reorder, `Enter`/`Space` visibility,
  `n` new, `d` duplicate, `x`/`Delete` delete (min 1 layer), `l` lock,
  `L` accepts-lighting, `Alt+S` cast-shadow, `+`/`-` opacity ±25,
  `b`/`B` blend Next/Prev, `Ctrl+G` group, `k` link-pair, `F2` rename,
  `m` merge-down (or mask-enabled toggle), `M` mask toggle, `,`/`.` move
  up/down
- [ ] Blend modes cycle Normal→Multiply→Overlay→Screen→Add→Subtract→Normal
  (`composite()` verified for opacity+blend+mask in unit tests)
- [ ] Hero `Multiply` over sky tints; `Screen` glows; hidden Sky leaves
  castle floating (visibility composes bottom-to-top)
- [ ] Locked Castle rejects brush strokes (F-09 guard, keyboard + drag)
- [ ] Save round-trip: File → Save as Figmap → reopen → byte-identical
  layers/names/order (unit: `figmap_roundtrip_image`). Notes:

## 3. Animation (`T` timeline, `A` add frame, `Shift+T` tween)

- [ ] `A` captures composite → `FN` frame, opens timeline on first capture
- [ ] `←`/`→` step frames (commits current, loads next); `Delete` drops
  frame (min 1); per-frame `delay` cs preserved
- [ ] Keyframe editor (`K`): pick layer/property, `Enter` edit, value commits
  to `get_interpolated_properties` lerp
- [ ] Tween (`Shift+T`): set start/end/count/easing (`↑↓←→`), `Enter`
  generates, `Enter`/`c` commits `tween N/M` frames, `Esc` discards
- [ ] Banner figmap: 6 frames × `document_state` + empty-derive rule both
  paths render under `--play`. Notes:

## 4. Lighting (`G` enters, `Esc` exits) + banner finale

Banner: `assets/e2e-art/banner-figby-finale.figmap` — real `fonts/big`
FIGBY in gold `#FFD700` + `░` halo, 6 twinkle/ember frames, ambient
`0.35 #8888FF` + point light sweeping x 8→56 (`LightKeyframe` t=0→1,
Linear), fps 8, loop on.

- [ ] `G` enters Lighting (auto-seeds ambient 0.5 white if scene empty);
  `↑/↓` select light, `←/→` move point-X, `Shift+↑/↓` move point-Y,
  `+`/`-` intensity ±0.1, `A`/`D`/`P` add ambient/directional/point,
  `Delete` removes, `Esc` exits to previous mode
- [ ] `--play banner --loop` shows shaded composite (Blinn-Phong via
  `compute_normal_map_figfont` + `shade_canvas`); keyframe time
  `frame_idx/frame_count` interpolates the sweep
- [ ] Export dialog (`Ctrl+E`): PNG/TXT/ANSI single; GIF/APNG/ANSI-multi
  animated when timeline present; `T` cycle, `L` per-layer, `P`
  transparency/play, `F` fps preset, `V` preview, `Space` step
- [ ] `figby --play sprite.figmap` (no frames) exits 1 with the
  no-frames message (verified). Notes:

## 5. File ops / docs / view (regression sweep)

- [ ] `Ctrl+N` new-image dialog (80×24 + palette picker); `Ctrl+O` Open
  (`.flf/.tlf/.figmap`, `..` + `←/→` nav, mouse click/wheel, zip browse);
  `Ctrl+S` save, `Ctrl+Shift+S` save-as (dir+filename split, `.flf`
  auto-append, figmap branch by extension); File → Save as Figmap (any
  mode); recent-files (10) on welcome + `1-9` jump
- [ ] `Ctrl+PageUp/PageDown` next/prev doc tab, `Ctrl+W` close (dirty →
  `y` save / `n` discard / `c` cancel); tab strip click switches
- [ ] `F5` Fast/Dirty render, `F11` zen (full canvas), `?` drawer,
  `Ctrl+K` keybindings overlay (arrows/`PageUp/Down` scroll, `Esc`/`q`
  close), `Ctrl+Shift+H` undo panel, `Ctrl+Shift+P` palette editor,
  `Alt+F/E/V/T/H` menus, `S` settings dialog
- [ ] Font editor: overview (`↑↓←→` nav, type-to-search, `Enter` edit,
  `A`dd, `D`el, `C`opy, `H`eader, `S`mush, `T`ransform); char editor
  (`Space` toggle, `M`irror, `F`lip, `G`enerate); preview `AaBbCc123!?`
- [ ] Image editor (`o` path, `c` color, `b/k/t/w` + `+`/`-` adjustments,
  `i` invert, `d` dither, `y` braille, `r` reset) + GIF import + rascii
  import (`assets/img/figby.png`, `cookies.gif`). Notes:

---

## Notes / Bugs (append rows while running)

| # | Date | Section | Issue | Severity |
|---|------|---------|-------|----------|
| 1 | 2026-09-23 | Export dialog |typed chars (`C-e`→`e`, `C-o`→`o`, `C-k`, `C-n`, `C-s`, `C-w`) land in `Path:` — dialog claims all keys; `Enter` on empty path runs async export `no path specified`, error reopens dialog. `Esc` closes only via async completion; keys pressed meanwhile keep appending to path. Fix: gate `handle_key` char arm while `path_buffer` empty + async in flight, or swallow `Ctrl+*` combos.| Medium |
| 2 | 2026-09-23 | Tool keys | `d` selects Eyedropper in ImageEditor but is swallowed in AsciiPreview (palette `show_extended` toggle runs first) — same key, different tool by mode. `c` Circle vs image-editor color, `k` Braille vs contrast/keyframe-editor, `r` Rotate vs text-rotate/reset — all mode/context shadowed. `m` Emitter and `n` Lighting have no canvas key path (mouse/menu only); `t` Text loses to timeline `T` exclusion + text-tool capture. Keymap table lists single keys; actual behavior is mode-dependent. | Low (docs) |
| 3 | 2026-09-23 | Palette vs tools | Palette claims `x/X, f/F, h/H, z/Z, arrows, Enter, Backspace, Esc` before tool selection — `f` Fill and `h`-adjacent flows unreachable while palette handles the key; verified `a` Spray also swallowed once Text tool active. Workaround: Esc/close palette first. | Low |
| 4 | 2026-09-23 | Welcome | `Enter` does NOT dismiss (only `Esc` + shortcut keys) though e2e-tmux script asserts it; `q` quits from welcome with no confirm. Script `scripts/e2e-tmux-test.sh` tests 2–7 all stale (welcome Enter, F11-zen grep, `e/f/l/b` tool greps, `C-o` Open grep, `C-k` overlay toggle). | Low (script) |
| 5 | 2026-09-23 | Figmap open | `Ctrl+O` is mode-dependent: ImageEditor routes to `enter_open_image` (image formats only), so `.figmap` open from image mode must go via `Alt+F` File menu or switch to FontEditor first. `start_open` (font `.flf/.tlf/.figmap`) vs `enter_open_image` (`.png/.jpg/.gif`) split is invisible in UI titles. | Low (docs) |
| 6 | 2026-09-23 | `--play` headless | No-PTY run fails `Device not configured (os error 6)` from `enable_raw_mode`; progress-bar fallback `terminal::size().unwrap_or((80,24))` exists only for the bar. Fine for a player, but the error string is raw io. | Trivial |
| 7 | 2026-09-23 | Emitter discoverability | No keyboard path to Emitter tool (`m` unbound on canvas; layer-panel `m` = merge-down). Only mouse click or Tools menu. Same for Lighting (`n` unbound; `G` enters *mode*, menu selects the *tool*). | Low (docs) |
|   |      |       |          |          |
